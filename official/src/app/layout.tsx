import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NekoCode - Terminal AI Assistant",
  description:
    "A cat-eared AI assistant in your terminal. Agent loop, multi-model, plugin ecosystem, sandbox security. One Go binary.",
  openGraph: {
    title: "NekoCode - Terminal AI Assistant",
    description:
      "A cat-eared AI assistant in your terminal. Agent loop, multi-model, plugin ecosystem, sandbox security.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-[var(--background)] text-[var(--foreground)]">
        {children}
      </body>
    </html>
  );
}
